# Keep "mock" for local preview. Submissions are stored in browser localStorage.
VITE_SUBMISSION_MODE=mock

# Set these values and change the mode to "supabase" after running supabase/schema.sql.
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_PUBLIC_ANON_KEY
