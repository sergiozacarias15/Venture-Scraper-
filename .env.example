# Server-only Pipedrive credentials. Never prefix these with VITE_.
PIPEDRIVE_API_TOKEN=
PIPEDRIVE_COMPANY_DOMAIN=
PIPEDRIVE_OWNER_ID=
PIPEDRIVE_PIPELINE_ID=2
# Optional. When empty, the endpoint resolves the exact stage name in pipeline 2.
PIPEDRIVE_STAGE_ID=
